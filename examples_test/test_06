program addition:
var sum, n: integer;

function sumF(a: integer): integer;
begin
    if a = 0 then return 0
    else return a + sumF(a - 1)
end sumF;


begin
   read(n);
   output (sumF(n), " sep ", n)
end addition.
