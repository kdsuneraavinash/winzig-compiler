program copy:
{ Echo-prints the first ten numbers on the input }
var count, f: integer;
begin
count := 1;
 while (count <= 10) do
    begin
       read(f);
       output(f);
       count := count + 1
    end
end copy.